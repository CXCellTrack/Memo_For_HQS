<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xchen.memo_for_hqs" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- my: add version and download_path -->
    <version>1.0.1</version>
    <download_path>https://github.com/CXCellTrack/Memo_For_HQS/releases/download/1.0.1/app-release.apk</download_path>

    <application
        android:allowBackup="true"
        android:icon="@drawable/sm"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Show_all_info"
            android:label="@string/title_activity_show_all_info" >
        </activity>
    </application>

</manifest>
