<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xchen.memo_for_hqs" >

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 我自己添加一个版本信息和下载地址 -->
    <version>1.0</version>
    <download_path>https://raw.githubusercontent.com/CXCellTrack/Memo_For_HQS/master/app/src/main/AndroidManifest.xml</download_path>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/myicon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Show_all_info"
            android:label="全部帐号信息" >
        </activity>
    </application>

</manifest>
